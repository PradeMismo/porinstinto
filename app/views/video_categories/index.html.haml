= render partial: 'layouts/navbar2'

#videos.container
  %br
  = bootstrap_flash
  %h2
    ="vídeos"
    .pull-right= link_to fa_icon("plus-circle"), new_video_category_path, :class => "btn btn-success" if current_user.try(:admin?) 
  %br   
  - @video_categories.each do |category|
    = render partial: 'video_table', locals: {category: category}
    %br 
    %hr.black
