= render partial: 'layouts/navbar2'

#concerts.container
  %br 
  = bootstrap_flash
  %h2
    ="conciertos pasados"
    .pull-right= link_to fa_icon("plus-circle"), new_concert_path, :class => "btn btn-success" if current_user.try(:admin?)
  %br 
  = render partial: 'last_concerts', locals: {concerts: @concerts} 
  %br  
  .text-center= link_to("próximos conciertos", concerts_path, class: "btn btn-danger") 